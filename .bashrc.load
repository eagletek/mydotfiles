export PATH=$PATH:$HOME/bin

set -o vi
set noclobber
set ignoreeof

eval $(dircolors ~/.dircolorsrc)

. ~/.git-completion.bash
. ~/.git-flow-completion.bash
. ~/.git-prompt.sh

. ~/.colors.bash
. ~/.prompt.bash

. ~/.move.bash
. ~/.print.bash
